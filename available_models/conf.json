{
    "models_root": "./available_models",
    "models": [
        {
            "id": 100,
            "model": "os2.pt",
            "timeout": 600,
            "on_timeout": "to_cpu",
            "load": true,
            "opt": {
                "beam_size": 30
            },
            "tokenizer": {
                "type": "pyonmttok",
                "mode": "conservative",
                "params": {"case_feature": true}
            }
        }
    ]
}
